extends layout

block content
  style.
    .contact-hero {
      text-align: center;
      padding: 3rem 0;
      background: linear-gradient(135deg, #2d6b3d 0%, #1e4d2b 100%);
      color: #f0ead6;
      border-radius: 8px;
      margin-bottom: 3rem;
    }
    
    .contact-hero h1 {
      font-size: 2.5rem;
      margin-bottom: 1rem;
      letter-spacing: 2px;
    }
    
    .contact-container {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 3rem;
      margin-top: 2rem;
    }
    
    .contact-info {
      background: white;
      padding: 2rem;
      border-radius: 8px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.05);
    }
    
    .contact-info h2 {
      color: #1e4d2b;
      margin-bottom: 2rem;
    }
    
    .info-item {
      display: flex;
      align-items: start;
      margin-bottom: 1.5rem;
      padding-bottom: 1.5rem;
      border-bottom: 1px solid #e0e0e0;
    }
    
    .info-item:last-child {
      border-bottom: none;
    }
    
    .info-icon {
      font-size: 1.5rem;
      margin-right: 1rem;
      color: #1e4d2b;
    }
    
    .info-content h3 {
      color: #1e4d2b;
      font-size: 1rem;
      margin-bottom: 0.3rem;
    }
    
    .info-content p {
      color: #666;
      margin: 0;
    }
    
    .contact-form {
      background: white;
      padding: 2rem;
      border-radius: 8px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.05);
    }
    
    .contact-form h2 {
      color: #1e4d2b;
      margin-bottom: 2rem;
    }
    
    .form-group {
      margin-bottom: 1.5rem;
    }
    
    .form-group label {
      display: block;
      color: #1e4d2b;
      font-weight: 600;
      margin-bottom: 0.5rem;
    }
    
    .form-group input,
    .form-group textarea {
      width: 100%;
      padding: 0.8rem;
      border: 2px solid #d4c5a9;
      border-radius: 6px;
      font-family: inherit;
      font-size: 1rem;
      transition: border-color 0.3s;
    }
    
    .form-group input:focus,
    .form-group textarea:focus {
      outline: none;
      border-color: #1e4d2b;
    }
    
    .form-group textarea {
      min-height: 150px;
      resize: vertical;
    }
    
    .submit-btn {
      width: 100%;
      padding: 1rem 2rem;
      background: #1e4d2b;
      color: #f0ead6;
      border: none;
      border-radius: 6px;
      font-size: 1.1rem;
      cursor: pointer;
      transition: background 0.3s;
    }
    
    .submit-btn:hover {
      background: #2d6b3d;
    }
    
    .social-links {
      margin-top: 2rem;
      text-align: center;
    }
    
    .social-links h3 {
      color: #1e4d2b;
      margin-bottom: 1rem;
    }
    
    .social-icons {
      display: flex;
      justify-content: center;
      gap: 1.5rem;
    }
    
    .social-icon {
      display: inline-block;
      width: 50px;
      height: 50px;
      background: #1e4d2b;
      color: #f0ead6;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.5rem;
      text-decoration: none;
      transition: transform 0.3s, background 0.3s;
    }
    
    .social-icon:hover {
      transform: scale(1.1);
      background: #2d6b3d;
    }
    
    @media (max-width: 768px) {
      .contact-container {
        grid-template-columns: 1fr;
      }
    }

  .contact-hero
    h1 📬 İletişim
    p Bizimle iletişime geçin
  
  .contact-container
    .contact-info
      h2 İletişim Bilgileri
      
      .info-item
        .info-icon 📍
        .info-content
          h3 Adres
          p Fatih Sultan Mehmet Mah.
          p Hat Sanatı Sokak No: 42
          p Üsküdar, İstanbul
      
      .info-item
        .info-icon 📞
        .info-content
          h3 Telefon
          p +90 (212) 555 44 33
          p +90 (532) 123 45 67
      
      .info-item
        .info-icon ✉️
        .info-content
          h3 E-posta
          p info@mesk.com.tr
          p destek@mesk.com.tr
      
      .info-item
        .info-icon 🕐
        .info-content
          h3 Çalışma Saatleri
          p Pazartesi - Cumartesi: 09:00 - 18:00
          p Pazar: Kapalı
      
      .social-links
        h3 Sosyal Medya
        .social-icons
          a.social-icon(href="#" aria-label="Instagram") 📷
          a.social-icon(href="#" aria-label="Facebook") 📘
          a.social-icon(href="#" aria-label="Twitter") 🐦
          a.social-icon(href="#" aria-label="WhatsApp") 💬
    
    .contact-form
      h2 Bize Yazın
      form#contactForm
        .form-group
          label(for="name") Adınız Soyadınız
          input#name(type="text" name="name" required)
        
        .form-group
          label(for="email") E-posta Adresiniz
          input#email(type="email" name="email" required)
        
        .form-group
          label(for="phone") Telefon Numaranız
          input#phone(type="tel" name="phone")
        
        .form-group
          label(for="subject") Konu
          input#subject(type="text" name="subject" required)
        
        .form-group
          label(for="message") Mesajınız
          textarea#message(name="message" required)
        
        button.submit-btn(type="submit") Gönder
  
  script.
    document.getElementById('contactForm').addEventListener('submit', function(e) {
      e.preventDefault();
      alert('Mesajınız alındı! En kısa sürede size dönüş yapacağız.');
      this.reset();
    });